@charset "utf-8"

@font-face
	font-family FusionPixel
	font-style normal
	src url(/assets/fusion-pixel-12px-proportional-zh_hans.woff2)
	 
/*
	::selection
		background var(--primary)
		color #fff
*/

*
	position relative
	box-sizing border-box
	background-clip padding-box !important
	tap-highlight-color transparent
	-webkit-tap-highlight-color transparent

html, body
	margin 0
	padding 0
	scroll-behavior smooth
	text-size-adjust 100%
	font-family FusionPixel, Roboto, HelveticaNeue, Arial, sans-serif

html.changing-theme
	&, *
		transition background 1s ease !important

a
	text-decoration none
	color var(--link)
	cursor pointer

	&:hover
		text-decoration underline

	*
		cursor pointer

@css {
	a {
		tap-highlight-color: var(--linkTapHighlight) !important;
		-webkit-tap-highlight-color: var(--linkTapHighlight) !important;
	}
}
